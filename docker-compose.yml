services:
  db:
    image: postgres:16-alpine
    container_name: reservas_db
    env_file:
      - .env
    environment:
      POSTGRES_DB: ${DATABASE_NAME:-reservas_db}
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD:-postgres}
      POSTGRES_INITDB_ARGS: "-E UTF8 --locale=C"
      DATABASE_USER: ${DATABASE_USER:-reservas_user}
    command:
      - "postgres"
      - "-c"
      - "shared_buffers=64MB"
      - "-c"
      - "effective_cache_size=192MB"
      - "-c"
      - "work_mem=4MB"
      - "-c"
      - "maintenance_work_mem=16MB"
      - "-c"
      - "max_connections=20"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db.sh:/docker-entrypoint-initdb.d/init-db.sh:ro
    # Puerto NO expuesto externamente - solo accesible entre contenedores en la misma red
    # Esto previene conflictos con otros servicios PostgreSQL en el servidor
    expose:
      - "5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d ${DATABASE_NAME:-reservas_db}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - reservas_network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
        reservations:
          cpus: '0.25'
          memory: 128M

  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: reservas_api
    volumes:
      - ./djangoapi:/app
      - static_volume:/app/static
      - media_volume:/app/media
      - logs_volume:/app/logs
    ports:
      - "${HOST_PORT:-8000}:8000"
    env_file:
      - .env
    environment:
      - DJANGO_SUPERUSER_USERNAME=${DJANGO_SUPERUSER_USERNAME:-admin}
      - DJANGO_SUPERUSER_EMAIL=${DJANGO_SUPERUSER_EMAIL:-admin@example.com}
      - DJANGO_SUPERUSER_PASSWORD=${DJANGO_SUPERUSER_PASSWORD:-admin}
      - AUTO_POPULATE_DB=${AUTO_POPULATE_DB:-False}
    depends_on:
      db:
        condition: service_healthy
    networks:
      - reservas_network
      - dokploy-network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
        reservations:
          cpus: '0.25'
          memory: 128M

volumes:
  postgres_data:
    driver: local
  static_volume:
    driver: local
  media_volume:
    driver: local
  logs_volume:
    driver: local

networks:
  reservas_network:
    driver: bridge
  dokploy-network:
    external: true
